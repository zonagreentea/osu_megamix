+let currentMode = null;
+
+function setMode(mode) {
+  currentMode = mode;
+}
+
+function gameLoop(t) {
+  if (currentMode === "osu") updateOsu(t);
+  if (currentMode === "catch") updateCatchMode(t);
+  if (currentMode === "taiko") updateTaiko(t);
+  if (currentMode === "mania") updateMania(t);
+
+  requestAnimationFrame(gameLoop);
+}
+
+requestAnimationFrame(gameLoop);
