# Builders

The mix is running.

- Build around the current moment (assume **now** is real).
- Don’t add global pause/reset behavior.
- If it requires stopping the music, reconsider it.

Branches:
- `builders/*` for experiments
- PRs flow **toward** `main`

See you next time.
