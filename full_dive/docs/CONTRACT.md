# Full Dive Contract (imagination*)

Goal:
- Prototype "full dive" UX + flow (front/back), without touching osu!megamix code.
- Export only docs/assets to megamix later.

Exports allowed:
- docs/*.md, assets/*, short clips, JSON seed data

Exports NOT allowed:
- engine source, build scripts, shared headers
